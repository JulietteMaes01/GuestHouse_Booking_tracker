# Credentials — never commit these
creds/
credentials.json
google-credentials.json
token.pickle

# Runtime files
# processed_emails.json is committed so GitHub Actions can track processed email IDs
__pycache__/
*.pyc
.ipynb_checkpoints/

# Data files
*.xlsx
.DS_Store
